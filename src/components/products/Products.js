import React from 'react';

const Products = (props) => {

    // console.log("product props", props)

    // console.log("product category", props.productsList.products )



    // let products = props.productsList.products.filter(product => product.id == props.match.params.id)[0]


    return(
        <div>
            Products by Category
        </div>
    )
}

export default Products;
